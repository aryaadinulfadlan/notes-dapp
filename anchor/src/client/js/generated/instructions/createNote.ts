/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { NOTESDAPP_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_NOTE_DISCRIMINATOR = new Uint8Array([
  103, 2, 208, 242, 86, 156, 151, 107,
]);

export function getCreateNoteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_NOTE_DISCRIMINATOR);
}

export type CreateNoteInstruction<
  TProgram extends string = typeof NOTESDAPP_PROGRAM_ADDRESS,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TAccountNote extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthor extends string
        ? WritableSignerAccount<TAccountAuthor> &
            AccountSignerMeta<TAccountAuthor>
        : TAccountAuthor,
      TAccountNote extends string
        ? WritableAccount<TAccountNote>
        : TAccountNote,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateNoteInstructionData = {
  discriminator: ReadonlyUint8Array;
  title: string;
  content: string;
};

export type CreateNoteInstructionDataArgs = { title: string; content: string };

export function getCreateNoteInstructionDataEncoder(): Encoder<CreateNoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['content', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_NOTE_DISCRIMINATOR })
  );
}

export function getCreateNoteInstructionDataDecoder(): Decoder<CreateNoteInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['content', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateNoteInstructionDataCodec(): Codec<
  CreateNoteInstructionDataArgs,
  CreateNoteInstructionData
> {
  return combineCodec(
    getCreateNoteInstructionDataEncoder(),
    getCreateNoteInstructionDataDecoder()
  );
}

export type CreateNoteAsyncInput<
  TAccountAuthor extends string = string,
  TAccountNote extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  note?: Address<TAccountNote>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: CreateNoteInstructionDataArgs['title'];
  content: CreateNoteInstructionDataArgs['content'];
};

export async function getCreateNoteInstructionAsync<
  TAccountAuthor extends string,
  TAccountNote extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOTESDAPP_PROGRAM_ADDRESS,
>(
  input: CreateNoteAsyncInput<
    TAccountAuthor,
    TAccountNote,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateNoteInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountNote,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOTESDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    note: { value: input.note ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.note.value) {
    accounts.note.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([110, 111, 116, 101])),
        getAddressEncoder().encode(expectAddress(accounts.author.value)),
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()).encode(
          expectSome(args.title)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.note),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateNoteInstructionDataEncoder().encode(
      args as CreateNoteInstructionDataArgs
    ),
    programAddress,
  } as CreateNoteInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountNote,
    TAccountSystemProgram
  >);
}

export type CreateNoteInput<
  TAccountAuthor extends string = string,
  TAccountNote extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  author: TransactionSigner<TAccountAuthor>;
  note: Address<TAccountNote>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: CreateNoteInstructionDataArgs['title'];
  content: CreateNoteInstructionDataArgs['content'];
};

export function getCreateNoteInstruction<
  TAccountAuthor extends string,
  TAccountNote extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NOTESDAPP_PROGRAM_ADDRESS,
>(
  input: CreateNoteInput<TAccountAuthor, TAccountNote, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): CreateNoteInstruction<
  TProgramAddress,
  TAccountAuthor,
  TAccountNote,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NOTESDAPP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    author: { value: input.author ?? null, isWritable: true },
    note: { value: input.note ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.author),
      getAccountMeta(accounts.note),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateNoteInstructionDataEncoder().encode(
      args as CreateNoteInstructionDataArgs
    ),
    programAddress,
  } as CreateNoteInstruction<
    TProgramAddress,
    TAccountAuthor,
    TAccountNote,
    TAccountSystemProgram
  >);
}

export type ParsedCreateNoteInstruction<
  TProgram extends string = typeof NOTESDAPP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    author: TAccountMetas[0];
    note: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateNoteInstructionData;
};

export function parseCreateNoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateNoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      author: getNextAccount(),
      note: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateNoteInstructionDataDecoder().decode(instruction.data),
  };
}
